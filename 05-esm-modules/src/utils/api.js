
const key = {
    value: 'AIzaSyD-EXAMPLE-KEY',
    permission: "admin"
}

async function fetchFromApi(endpoint) {
    //logica de conexão com a api
    console.log(`Buscando dados do endpoint: ${endpoint}`);
}

export {
    key,
    fetchFromApi
};